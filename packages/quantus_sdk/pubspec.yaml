name: quantus_sdk
description: Shared Quantus Dart + Rust bindings and services
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

# Convention: see dependency placement section in ../melos.yaml.
# Shared dep versions must match the canonical list there.
dependencies:
  flutter:
    sdk: flutter

  rust_lib_quantus_wallet:
    path: ./rust_builder
  flutter_rust_bridge: ^2.12.0

  # Shared (canonical versions in melos.yaml)
  # DO NOT UPDATE polkadart - newer versions are incompatible with our ML-DSA
  # signature override, work completely differently, and add no benefits.
  #
  # The fork also carries a security fix to the code generator. Upstream emits a
  # compact integer for the MultiAddress::Index variant where the metadata
  # declares the field zero-width. A codec that disagrees with the metadata about
  # a field's width re-frames every byte after it, so a crafted payload can
  # display one call and sign another - a clearsigning bypass on the cold wallet.
  # Pinned by test/chain/multi_address_codec_test.dart. Never regenerate the
  # bindings with pub.dev polkadart_cli.
  # Re-exported from quantus_sdk.dart so apps don't need a direct polkadart dep.
  polkadart:
    git:
      url: https://github.com/Quantus-Network/polkadart.git
      ref: 0_7_3_quantus_2
      path: packages/polkadart
  bip39_mnemonic: ^4.0.1
  convert: ^3.1.2
  http: ^1.6.0
  shared_preferences: ^2.5.5
  decimal: ^3.2.4
  path: ^1.9.1
  flash: ^3.1.1
  collection: ^1.19.1

  # SDK-only
  ss58:
    git:
      url: https://github.com/Quantus-Network/polkadart.git
      ref: 0_7_3_quantus_2
      path: packages/ss58
  intl: ^0.20.2
  flutter_secure_storage: ^10.1.0
  connectivity_plus: ^7.1.1
  path_provider: ^2.1.5

  human_checksum:
    git:
      url: https://github.com/Quantus-Network/qp-human-checkphrase.git
      ref: v2.2.0
      path: dart

  qr_flutter: ^4.1.0
  flutter_svg: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  path_provider_platform_interface: ^2.1.0
  plugin_platform_interface: ^2.1.0
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # Used by tool/generate_call_corpus.dart to enumerate the runtime's calls.
  polkadart_scale_codec:
    git:
      url: https://github.com/Quantus-Network/polkadart.git
      ref: 0_7_3_quantus_2
      path: packages/polkadart_scale_codec
  substrate_metadata:
    git:
      url: https://github.com/Quantus-Network/polkadart.git
      ref: 0_7_3_quantus_2
      path: packages/substrate_metadata
  # DO NOT UPDATE polkadart_cli - must match polkadart 0.7.x, and generates the
  # MultiAddress::Index codec correctly. See note above.
  polkadart_cli:
    git:
      url: https://github.com/Quantus-Network/polkadart.git
      ref: 0_7_3_quantus_2
      path: packages/polkadart_cli
polkadart:
  output_dir: lib/generated
  chains:
    planck: wss://a1-planck.quantus.cat

flutter:
  assets:
    - assets/v2/uppercase_q_black_bg.png
    - assets/icons/
